---
import Page from "../../blog.astro";
import { getStaticPaths as proxyGetStaticPaths } from "../../blog.astro";
/* @ts-ignore */
export const getStaticPaths = (props) =>
  proxyGetStaticPaths({
    ...props,
    astroI18n: {
      locale: "es",
      route: "/blog",
      primaryLocale: "en",
      secondaryLocales: ["es"],
    },
  });
const { props } = Astro;
---

<Page {...props} />
